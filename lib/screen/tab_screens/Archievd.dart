import 'package:flutter/material.dart';

class ArchievdScreen extends StatelessWidget {
  const ArchievdScreen({super.key});

  @override
  Widget build(BuildContext context) {
   return Scaffold(
    body: Column(
      children: [  Text('archied')],
    ),
   );
  }
}